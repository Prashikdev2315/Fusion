# Backend production environment example
APP_ENV=production
# Keep production logs strict; use WARNING only if operationally required
LOG_LEVEL=ERROR
SECRET_KEY=replace-with-production-secret
MAIL_PASSWORD=replace-with-production-mail-password
